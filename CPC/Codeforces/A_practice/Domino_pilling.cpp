#include <bits/stdc++.h>

using namespace std;

#define cpu() ios::sync_with_stdio(false); cin.tie(nullptr);

void solve(){
  int n, m;
  cin>>n>>m;

  cout << (n*m)/2;
}

int main(){
  solve();

  return 0;
}
